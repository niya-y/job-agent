# ============================================
# HUGGINGFACE API CONFIGURATION (REQUIRED)
# ============================================
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here


# ============================================
# MODEL SELECTION
# ============================================

# --- Embedding Model (for resume matching) ---
# Recommended: Snowflake Arctic (best quality/speed balance)
# Alternatives:
#   - sentence-transformers/all-MiniLM-L6-v2 (lightweight)
#   - BAAI/bge-base-en-v1.5 (high quality)
#   - dunzhang/stella_en_1.5B_v5 (very accurate but slower)
HF_EMBED_MODEL=Snowflake/snowflake-arctic-embed-m

# --- Text Generation Model (for cover letters) ---
# Recommended: Mistral-7B (most stable in free tier)
# Alternatives (try if Mistral fails):
#   - HuggingFaceH4/zephyr-7b-beta (good fallback)
#   - meta-llama/Meta-Llama-3.1-8B-Instruct (requires approval)
HF_TEXT_MODEL=HuggingFaceH4/zephyr-7b-beta"

# --- JD Extraction Model ---
# JobSpanBERT is used automatically (no API needed)
# You can change this in agents/jd_extractor.py if needed


# ============================================
# MATCHING CONFIGURATION
# ============================================

# Number of top matching experiences to retrieve
TOP_K=6

# Minimum similarity threshold (0.0-1.0)
# Lower = more results, Higher = more relevant
MIN_SIMILARITY=0.3


# ============================================
# GENERATION SETTINGS
# ============================================

# Cover letter generation mode
# Options: auto | rule | huggingface
# - auto: tries HF model, falls back to rule-based
# - rule: always use template-based generation
# - huggingface: always use LLM (may fail if quota exceeded)
GENERATION_MODE=auto

# Temperature for text generation (0.0-1.0)
# Lower = more focused, Higher = more creative
TEMPERATURE=0.7

# Maximum tokens for generated cover letter
MAX_TOKENS=800


# ============================================
# OPTIONAL: OPENAI API (Not required)
# ============================================
# If you prefer OpenAI models over HuggingFace
# Uncomment and add your key:
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4o-mini

# Use OpenAI for embedding (alternative to HF)
# EMBED_PROVIDER=openai
# EMBED_MODEL=text-embedding-3-small


# ============================================
# ADVANCED SETTINGS
# ============================================

# Enable debug logging
DEBUG=false

# Save intermediate results
SAVE_INTERMEDIATE=true

# Output directory for generated files
OUTPUT_DIR=./outputs
